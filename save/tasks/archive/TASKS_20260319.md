# Tasks - cmux-ai project - 2026-03-19

## Completed Tasks

- ~~All tasks from sessions 1-3 (see TASKS_20260318*.md)~~
- ~~Background image overlay system using child NSWindow~~
  - Child NSWindow floats above main window with `ignoresMouseEvents = true`
  - Image loaded from `~/.config/cmux/terminal-bg.png` (bypasses Xcode asset catalog caching)
  - CALayer with `.resizeAspectFill` for proper aspect-fill scaling
  - 35% opacity, full window coverage
  - Auto-syncs position/size on window resize/move
  - Cleans up on window close
- ~~Fixed usage panel layering (moved to zIndex above terminal)~~
- ~~Tried multiple approaches for background: SwiftUI ZStack (failed - AppKit portals paint over), NSImageView behind window (failed - terminal opaque), NSImageView overlay (working)~~
- ~~Reverted Ghostty opacity hacks (defaultBackgroundOpacity changes, opacity cap) - not needed~~

## Remaining Tasks

- **Commit and push all changes** - uncommitted changes: background image overlay, panel layering fixes, sci-fi renderer code (unused but still in file)
- **Clean up unused code**: SciFiRenderer, SeededRNG, TerminalMediaBackground struct (now unused since we use child window approach)
- **Clean up asset catalog**: Remove TerminalBackground.imageset (no longer used, loading from ~/.config/cmux/)
- **Usage panel testing**: Verify ClaudeUsagePanel and ClaudeUsageStatusBar work correctly with the new layering
- **Pane icon improvement**: Unicode U+273B in bonsplit still looks like Gemini logo
