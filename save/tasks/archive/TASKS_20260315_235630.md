# Voice Assistant 2.0 - Performance Optimization Tasks
## Date: 2026-03-15
## Project: /Users/dzineer/Clients/Dzineer/Projects/voice-assistant-2.0

---

## Completed Tasks

~~**#6 - React Memoization**~~
- Wrapped 5 key components with `React.memo`: ChatMessage, ChatHistory, SciFiBackground, MicButton, VoiceStatus
- Files changed: `frontend/src/components/ChatMessage.jsx`, `ChatHistory.jsx`, `SciFiBackground.jsx`, `MicButton.jsx`, `VoiceStatus.jsx`
- Changed `export function X` to `export const X = memo(function X`
- Added `import { memo } from "react"` to each
- Frontend builds clean (vite build passes)
- Test: `backend/tests/test_react_memoization.py` (7 tests)

~~**#7 - WebSocket Broadcasts to ALL Clients**~~
- Upgraded `ConnectionManager` in `backend/routes/websocket.py` with:
  - Stream ownership tracking (`_stream_owners` dict maps stream_id -> WebSocket)
  - Session-filtered broadcasting (stream events only go to owning client)
  - 50ms chunk batching (`_batch_buffers`, `_delayed_flush`)
  - Context variable `_current_websocket` to pass originating WebSocket through to `process_chat`
- Updated `backend/routes/chat.py`:
  - Registers stream owner when stream_id created
  - Unregisters on stream completion
  - All stream broadcasts now include `stream_id` for routing
- Test: `backend/tests/test_websocket_broadcast.py` (7 tests)

~~**#8 - FTS5 Index Rebuilt on Every Startup**~~
- Added `KgIndexState` model to `backend/models.py` (tracks `last_indexed_at` watermark per content_type)
- Made `index_existing_content()` in `backend/services/rag_service.py` incremental (queries only items with created_at/updated_at > watermark)
- Made `migrate_existing_content()` in `backend/services/knowledge_graph_service.py` incremental (same watermark approach for ChromaDB)
- Updated `backend/main.py`:
  - Creates `kg_index_state` table on startup
  - FTS5 indexing now runs every startup (incremental), not just when empty
- Test: `backend/tests/test_incremental_indexing.py` (7 tests)

~~**#9 - Quick Wins (all 5)**~~
- **9.1 TTS length_scale**: Changed from 0.95 to 0.85 in `backend/services/tts_service.py`
- **9.2 Voice preloading**: Added `preload_voices()` function in `tts_service.py`, called at startup in `main.py`
- **9.3 Auto-compact threshold**: Changed from 40% to 25% in `backend/routes/chat.py`
- **9.4 HTTP connection pooling**: Added `_client_cache` dict and `_get_cached_openai_client()`, `_get_cached_async_openai_client()`, `_get_cached_anthropic_client()` in `backend/services/llm_service.py`. Replaced all client creation calls.
- **9.5 RAG context**: Reduced `MAX_RAG_CONTEXT_CHARS` from 6000 to 4000 in `backend/services/rag_service.py`. Added per-result relevance-weighted capping (`per_result_cap`).
- Updated `backend/tests/test_auto_compact.py` to match new 25% threshold
- Test: `backend/tests/test_quick_wins.py` (9 tests)

---

## Remaining Tasks (Not Started)

### #1 - Chunked Streaming TTS (HIGHEST PRIORITY)
- Currently audio synthesis waits for full LLM response
- Fix: Split on sentence boundaries during LLM streaming, synthesize audio chunks in parallel
- Start playing first sentence while rest generates
- Expected: 50-70% reduction in perceived latency
- Key files: `backend/services/tts_service.py`, `backend/routes/tts.py`, `backend/routes/chat.py`

### #2 - Parallel Context Building with asyncio.gather()
- Currently serial: history loading, RAG, file contexts, memories all sequential
- Fix: Use `asyncio.gather()` to fetch all in parallel
- Key file: `backend/routes/chat.py` (the `process_chat` function)

### #3 - Database Indexes + Paginated History
- 8.4 GB SQLite with no indexes on hot columns
- Fix: Add indexes on `messages(session_id, created_at)`, `transcripts(status)`, etc.
- Key files: `backend/models.py`, `backend/main.py` (migration section)

### #4 - Full Chat History Loaded Every Request
- No pagination - loads all messages, then token-counts
- Fix: Sliding window - only fetch last N messages up to context budget
- Key files: `backend/routes/chat.py`, `backend/services/llm_service.py`

### #5 - Synchronous File I/O in Async Handlers
- `_encode_image`, `PiperVoice.load`, disk reads block event loop
- Fix: Wrap in `asyncio.to_thread()` or use `aiofiles`
- Key files: `backend/services/llm_service.py`, `backend/services/tts_service.py`

---

## Test Summary
- **79/79 tests passing** (1 pre-existing broken: `test_service_tag_routing.py`)
- Test catalog: `backend/TESTS.md`
- Run all: `cd backend && python -m pytest tests/ -v --ignore=tests/test_service_tag_routing.py`
