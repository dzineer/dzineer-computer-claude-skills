# Voice Assistant 2.0 - Performance Optimization Tasks
## Date: 2026-03-16

## Project
- **Path**: `/Users/dzineer/Clients/Dzineer/Projects/voice-assistant-2.0`
- **Working Dir (Amy)**: `/Users/dzineer/Clients/Dzineer/Projects/local_deployments/cb56e703-374f-415e-9ffa-6d545a130364/orchestrator`
- **Test command**: `cd voice-assistant-2.0/backend && python -m pytest tests/ --ignore=tests/test_service_tag_routing.py -q`
- **Current test count**: 88/88 passing

---

## Completed Tasks

~~**#8 - FTS5 index rebuilt on every startup**~~
- Added `KgIndexState` model to `models.py` for watermark tracking
- Made `index_existing_content()` in `rag_service.py` incremental (query by `created_at > watermark`)
- Made `migrate_existing_content()` in `knowledge_graph_service.py` incremental
- Updated `main.py` to create `kg_index_state` table and always run incremental indexing
- Test: `test_incremental_indexing.py` (7 tests)

~~**#9 - Quick Wins (all 5)**~~
- QW1: `tts_service.py` length_scale 0.95 -> 0.85
- QW2: `tts_service.py` added `preload_voices()`, wired into `main.py` startup
- QW3: `chat.py` auto-compact threshold 40% -> 25%
- QW4: `llm_service.py` HTTP connection pooling via `_get_cached_openai_client`, `_get_cached_async_openai_client`, `_get_cached_anthropic_client`
- QW5: `rag_service.py` MAX_RAG_CONTEXT_CHARS 6000 -> 4000, added per-result relevance decay
- Test: `test_quick_wins.py` (9 tests)

~~**#7 - WebSocket broadcasts to ALL clients**~~
- Rewrote `ConnectionManager` in `websocket.py` with stream ownership tracking
- Stream events (stream_start, stream_chunk, stream_end, filler_trigger, stream_cancelled) only go to owning client
- Added 50ms chunk batching (_enqueue_chunk, _delayed_flush, _flush_chunks)
- Used `contextvars.ContextVar` to pass originating WebSocket through to `process_chat`
- Added `stream_id` to all broadcast messages for proper routing
- Cleanup on disconnect
- Test: `test_websocket_broadcast.py` (7 tests)

~~**#6 - No React memoization**~~
- Wrapped with `React.memo`: ChatMessage, ChatHistory, SciFiBackground, MicButton, VoiceStatus
- All in `frontend/src/components/`
- Frontend builds clean (`npx vite build`)
- Test: `test_react_memoization.py` (7 tests)

~~**#5 - Synchronous file I/O in async handlers**~~
- `llm_service.py`: Split `_encode_image` into sync `_encode_image_sync` + async wrapper using `asyncio.to_thread`
- `llm_service.py`: Made `_build_messages` async to support `await _encode_image`
- `routes/tts.py`: Wrapped `synthesize()` in `asyncio.to_thread`
- `routes/chat.py`: Wrapped image `write_bytes` in `asyncio.to_thread`
- `routes/security.py`: Updated `_encode_image` call to `await`
- Updated `test_build_messages.py` to use async/await
- NOTE: There was a bug found and fixed by another agent after commit
- Test: `test_async_file_io.py` (5 tests)

~~**#4 - Full chat history loaded every request**~~
- `routes/chat.py` `process_chat()`: Changed from `.all()` to `.order_by(desc()).limit(50)` + reverse
- `_build_messages` already had `history[-50:]` sliding window as safety net
- Test: `test_history_pagination.py` (4 tests)

---

## Remaining Tasks

### #1 - Chunked streaming TTS (HIGHEST PRIORITY)
- Currently audio synthesis waits for full LLM response
- Fix: Split on sentence boundaries as LLM tokens stream in, synthesize audio chunks in parallel
- Start playing first sentence while rest generates
- Expected: 50-70% perceived latency reduction
- Files to modify: `routes/chat.py` (_handle_streaming_chat), `services/tts_service.py`, `routes/websocket.py`

### #2 - Parallel context building (asyncio.gather)
- Currently serial: history load, file contexts, RAG query, memories all sequential
- Fix: Use `asyncio.gather()` to fetch all in parallel
- Files: `routes/chat.py` (process_chat, lines ~522-570)

### #3 - Database indexes + paginated history
- No indexes on hot columns
- Fix: Add indexes on `messages(session_id, created_at)`, `transcripts(status)`, etc.
- Files: `models.py`, possibly an alembic migration or `main.py` startup

---

## Key File Locations

| File | What Changed |
|------|-------------|
| `backend/models.py` | Added `KgIndexState` model |
| `backend/services/rag_service.py` | Incremental FTS5, watermarks, MAX_RAG=4000, relevance decay |
| `backend/services/knowledge_graph_service.py` | Incremental KG indexing, watermarks |
| `backend/services/llm_service.py` | Cached HTTP clients, async _encode_image, async _build_messages |
| `backend/services/tts_service.py` | length_scale=0.85, preload_voices() |
| `backend/routes/chat.py` | History limit(50), compact threshold 25%, async image write, stream_id on all broadcasts |
| `backend/routes/tts.py` | asyncio.to_thread for synthesize |
| `backend/routes/websocket.py` | Stream ownership, chunk batching, contextvars |
| `backend/routes/security.py` | await _encode_image |
| `backend/main.py` | kg_index_state table, preload_voices at startup |
| `frontend/src/components/ChatMessage.jsx` | React.memo |
| `frontend/src/components/ChatHistory.jsx` | React.memo |
| `frontend/src/components/SciFiBackground.jsx` | React.memo |
| `frontend/src/components/MicButton.jsx` | React.memo |
| `frontend/src/components/VoiceStatus.jsx` | React.memo |
| `backend/TESTS.md` | Full test catalog |

## Test Files Added
- `test_incremental_indexing.py` (7) - #8
- `test_quick_wins.py` (9) - #9
- `test_websocket_broadcast.py` (7) - #7
- `test_react_memoization.py` (7) - #6
- `test_async_file_io.py` (5) - #5
- `test_history_pagination.py` (4) - #4
