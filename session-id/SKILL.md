---
name: session-id
description: Reads from the current claude code session and returns the claude
session id
disable-model-invocation: true
---

Read the current context, retreive the current Claude Session, and return the
Claude session id. The output should just be {claude_session_id} where that is
just the session id and nothing else, no additional space, no bullet point
characters, just the claude session id.
